{
  "extends": "../default/tsconfig-base.json",
  "compilerOptions": {
    // // comment in this line to get static assets into dist
    // // comment out to see them show up in the root of the mono repo
    // "outDir": "../../../../dist",
    "declaration": false,
    "declarationMap": false
  }
}
